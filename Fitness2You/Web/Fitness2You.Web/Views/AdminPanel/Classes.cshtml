@model IList<ClassesInputViewModel>
@{
    ViewData["Title"] = "Classes page";
}

<main class="site-content admin">
    @if (this.User.IsInRole("Admin"))
    {
        <partial name="AdminNavigationBar/_AdminNavigationBarClassActive" />
        <section class="site-wrapper">
            <section class="main">
                <section class="add-section">
                    <a asp-controller="AdminPanel" asp-action="AddClass">
                        <i class="fas fa-plus-circle"></i>
                    </a>
                </section>
                <section class="classes">
                    @foreach (var classes in Model)
                    {
                        <article class="class">
                            <section class="class-img">
                                <img src="@classes.ImageUrl" alt="Image not found!">
                            </section>
                            <section class="class-name">@classes.Name</section>
                            <section class="class-price">@classes.Price €</section>
                            <section class="class-discount">@classes.Discount %</section>
                            @if (classes.IsActive == true)
                            {
                                <section class="class-status">Active!</section>
                            }
                            else
                            {
                                <section class="class-status">No Active!</section>
                            }
                            <section class="card-actions">
                                <a asp-controller="AdminPanel" asp-action="EditClass" asp-route-id="@classes.Id" class="edit">Edit</a>
                                <a asp-controller="AdminPanel" asp-action="DeleteClass" asp-route-id="@classes.Id" class="delete">Delete</a>
                            </section>
                        </article>
                    }
                </section>
            </section>
        </section>
    }
</main>